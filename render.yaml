services:
  - type: web
    name: policylens-ai-backend
    runtime: python312
    plan: starter
    branch: main
    rootDir: ./
    buildCommand: pip install -r backend/requirements.txt && python -m spacy download en_core_web_sm
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite+aiosqlite:///./policylens.db
      - key: GROQ_API_KEY
        value: ""
      - key: OLLAMA_BASE_URL
        value: ""
      - key: LIBRETRANSLATE_URL
        value: ""
